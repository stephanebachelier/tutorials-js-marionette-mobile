
  * add unit test for:
    * views:
      * MovieListItemView: serializeData
      * MovieDetailsView: serializeData (borrow from MovieListItemView)
    * controllers:
      * NavController:
        * list()
        * showMovie()
